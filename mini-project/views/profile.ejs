<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body>
    <div class="w-full min-h-screen bg-zinc-900 p-10">
        <div class="flex w-full justify-end p-5">


            <a href="/logout" class="bg-red-500 rounded-md px-3 py-2 text-sm text-white inline-block mb-5">Logout</a>
        </div>
        <h1 class="text-3xl mb-3 text-white">Hello <%=user.name%> 👋
        </h1>
        <h5 class="text-white mb-5">you can create a new post.</h5>
        <form action="/post" method="post">
            <textarea name="content" placeholder="what's on your mind?"
                class="w-1/3 resize-none bg-zinc-800 text-white p-3 outline-none rounded-md"></textarea>
            <input type="submit" value="Create new post"
                class="px-3 w-40 py-2 bg-blue-500 block rounded-md mt-2 text-sm text-white">
        </form>

        <div class="posts mt-20">
            <div class="text-zinc-400">Your Posts</div>
            <div class="postcontainer mt-5 flex gap-5 flex-wrap">
                <% user.post.reverse().forEach((post)=>{%>

                    <div class="post w-1/3 mb-4 p-4 rounded-md border--[1px] border-zinc-700 bg-zinc-800">
                        <h4 class="text-blue-500 mb-3">@<%=user.username%>
                        </h4>
                        <p class="text-white">
                            <%=post.content%>
                        </p>
                        <small class="mt-2 text-zinc-400 inline-block">
                            <%=post.likes.length%> Likes
                        </small>
                        <div class="btn flex mt-5 gap-5">
                            <a href="/like/<%=post._id%>" class="text-blue-500 ">

                                <%= post.likes.indexOf(user._id) ? "Like" : "Unlike" %>
                            </a>
                            <a href="/edit/<%=post._id%>" class="text-zinc-600">Edit</a>
                        </div>
                    </div>
                    <% })%>

            </div>
        </div>
    </div>
</body>

</html>